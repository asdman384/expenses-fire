name: build

on:
  push:
    branches: ['master']
  pull_request:
    branches: ['master']

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      
      - name: Build      
           
        run: |
          # echo "${{ secrets.API_KEY }}"
          # echo "${{ vars.API_KEY2 }}"
          echo '{  "API_KEY2": "$vars:API_KEY2" }'
